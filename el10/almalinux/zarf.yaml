---
# yaml-language-server: $schema=https://raw.githubusercontent.com/zarf-dev/zarf/main/zarf.schema.json
kind: ZarfPackageConfig
metadata:
  name: el10-alma
  version: "###ZARF_PKG_TMPL_VERSION###"
components:
  - name: enterprise-linux
    required: true
    only:
      flavor: bootc
    images:
      - ghcr.io/a1994sc/bootc-images/bootc/alma10:latest
  - name: enterprise-linux
    required: true
    only:
      flavor: rpm
    images:
      - ghcr.io/a1994sc/bootc-images/rpm/alma10:latest
