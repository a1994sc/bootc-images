---
# yaml-language-server: $schema=https://raw.githubusercontent.com/zarf-dev/zarf/main/zarf.schema.json
kind: ZarfPackageConfig
metadata:
  name: el09-rocky
  version: "###ZARF_PKG_TMPL_VERSION###"
components:
  - name: enterprise-linux
    required: true
    only:
      flavor: bootc
    images:
      - ghcr.io/a1994sc/bootc-images/bootc/rocky9:latest
  - name: enterprise-linux
    required: true
    only:
      flavor: rpm
    images:
      - ghcr.io/a1994sc/bootc-images/rpm/rocky9:latest
